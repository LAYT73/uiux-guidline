export type { Rule, Section, SectionId, Topic, TopicId } from './model/types'
export {
  RULES,
  SECTIONS,
  TOPICS,
  getRulesByTopic,
  getSection,
  getTopic,
  getTopicById,
  isSectionId,
  isTopicId,
} from './model/catalog'
